<!DOCTYPE html>
<html lang="en">

  <head>
    <meta charset="UTF-8">
    <title>Members Only Project - Jamal J8 Ahmed</title>
  </head>

  <body>
    <h1>Home Page</h1>

    <div>
      <h1>Messages GO HERE</h1>
    </div>

    <% if (currentUser) { %>
      <h1>WELCOME BACK <%= currentUser.username %></h1>
      <a href="/add-message">Add New Message</a>
      <a href="/log-out">LOG OUT</a>
    <% } else { %>
      <a href="/login">LOGIN</a>
    <% } %>

    <% if (messages) { %>
      <h2>Messages:</h2>
      <ul>
	<% messages.map(msg => { %>

	  <li><%= msg.title %></li>

	<% }) %>

      </ul>
    <% } %>

  </body>

</html>

